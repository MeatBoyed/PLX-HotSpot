# RadiusDesk Server
RD_HOST=102.213.201.130
RD_SSH_USER=charlesr
RD_SSH_KEY_PATH=~/.ssh/id_ed25519
RD_SSH_PASSWORD=ChangeM3!         

# MikroTik Router (password auth only)
MT_API_HOST=gateway.pluxnet.co.za
MT_USER=admin
MT_PASSWORD=SystemMan2025!!

# MariaDB (accessed via SSH tunnel through RD_HOST)
DB_NAME=rd
DB_USER=root
DB_PASSWORD=
DB_PORT=3306               # local port on RadiusDesk VM

# Analysis settings
LOG_HOURS_DEFAULT=24       # how many hours of logs to analyse
ACCOUNTING_WARN_PPM=30     # packets/min threshold before warning
CERT_WARN_DAYS=30          # warn when cert expires within N days
CERT_CRITICAL_DAYS=7       # critical when cert expires within N days

# Report output directory
REPORT_DIR=reports
