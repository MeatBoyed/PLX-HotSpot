# Copy to .env and fill with your DB connection string
# MariaDB example:
# DATABASE_URL="mysql://user:pass@radiusdesk.pluxnet.co.za:3306/rd"
DATABASE_URL=""

# HTTP port the Hono app listens on inside the container
PORT=3000

# Optional: host port to bind (defaults to same as PORT if unset)
# HOST_PORT=3000
RADIUS_DATABASE_URL=""
APP_DATABASE_URL=""
ALLOWED_ORIGINS=""
