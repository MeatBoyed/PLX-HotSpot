services:
  # ── AuraConnect ────────────────────────────────────────────────────────────

  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "0.0.0.0:5299:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Host=postgres;Port=5432;Database=auraconnect_test;Username=local_dev;Password=KQP2Hqg16gqp5u
    depends_on:
      postgres:
      condition: service_healthy

  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: auraconnect_test
      POSTGRES_USER: local_dev
      POSTGRES_PASSWORD: KQP2Hqg16gqp5u
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-db.sql:/docker-entrypoint-initdb.d/init-db.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U local_dev -d auraconnect_test"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ── Blnk Finance ───────────────────────────────────────────────────────────

  # blnk-server:
  #   image: jerryenebeli/blnk:0.10.2
  #   restart: on-failure
  #   entrypoint: ["/bin/sh", "-c"]
  #   command:
  #     - "blnk migrate up && blnk start"
  #   ports:
  #     - "5001:5001"
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy
  #     typesense:
  #       condition: service_started
  #   volumes:
  #     - ./blnk.json:/blnk.json

  # blnk-worker:
  #   image: jerryenebeli/blnk:0.10.2
  #   restart: on-failure
  #   entrypoint: ["blnk", "workers"]
  #   depends_on:
  #     - blnk-server
  #   volumes:
  #     - ./blnk.json:/blnk.json

  # redis:
  #   image: redis:7.2.4
  #   restart: unless-stopped
  #   healthcheck:
  #     test: ["CMD", "redis-cli", "ping"]
  #     interval: 5s
  #     timeout: 3s
  #     retries: 5

  # typesense:
  #   image: typesense/typesense:0.23.1
  #   restart: unless-stopped
  #   command: ["--data-dir", "/data", "--api-key=blnk-api-key", "--listen-port", "8108"]
  #   volumes:
  #     - typesense_data:/data

volumes:
  postgres_data:
  # typesense_data:
