// Empty stub for the `server-only` package under Vitest.
// `server-only` throws when imported outside an RSC bundle; tests run server-side
// logic directly, so we alias it to this no-op (see vitest.config.ts).
export {};
